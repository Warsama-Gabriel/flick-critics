<div class="card" style="width: 16rem; margin-bottom: 10px;">
  <div class="card-body">
    <h5 class="card-title"><%= recent_reviews.title %></h5>
    <h6 class="card-title">reviewed on: <%= recent_reviews.created_at.strftime("%b, %d %Y") %></h6>
    <h6 class="card-subtitle mb-2 text-muted">by: <%= recent_reviews.email %></h6>
    <h6 class="card-subtitle mb-2 text-muted">rating: <%= generate_rating recent_reviews.rating %> </h6>
    <p class="card-text"><i>"<%= recent_reviews.body %>"</i></p>
  </div>
</div>